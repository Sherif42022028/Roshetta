# خطة بناء: تطبيق متابعة عيادات مميز بميزات AI حقيقية

## 1. الفكرة والتمييز

منتج إدارة عيادات (حجز + ملف مريض + فواتير) بنفس القاعدة اللي المنافسين المصريين (Yolo, Tabbaba, Medicakare, ClinicGateway) بيقدموها، لكن بميزتين AI حقيقيتين محدش من المنافسين المصريين بيقدمهم حاليًا:

1. **ملاحظات طبية بالصوت بالعربي المصري (Ambient Clinical Notes)**
2. **تنبؤ بالغياب + تذكير واتساب ذكي (No-show Prediction)**

**قاعدة أساسية:** الميزتين دول أدوات مساعدة للطبيب، مش بديل عن قراره الطبي. الـAI بيسوّد (Draft) والطبيب بيراجع ويعتمد — لا يوجد حفظ تلقائي لأي بيانات طبية من غير مراجعة بشرية.

---

## 2. الميزة الأولى: ملاحظات طبية بالصوت

### الفكرة
الطبيب يتكلم عادي أثناء الكشف، والنظام يحوّل كلامه لملف مريض منظم (الشكوى، التشخيص، الروشتة، الخطة العلاجية) تلقائيًا.

### التنفيذ التقني
```
تسجيل صوتي (بموافقة المريض الصريحة) 
    ↓
Whisper (OpenAI، مفتوح المصدر، يدعم العربي) — تحويل الصوت لنص خام
    ↓
Claude API — هيكلة النص الخام لحقول منظمة (شكوى، تشخيص، روشتة، خطة)
    ↓
عرض المسودة للطبيب للمراجعة والتعديل
    ↓
حفظ نهائي بعد اعتماد الطبيب فقط
```

### متطلبات إلزامية (خصوصية بيانات صحية حساسة)
- **موافقة صريحة من المريض قبل أي تسجيل** — شاشة موافقة واضحة، مش افتراضية (Opt-in لا Opt-out)
- **التسجيل الصوتي الخام يُحذف فورًا بعد التحويل للنص** — لا يُخزن الصوت نفسه على المدى الطويل، فقط النص المهيكل بعد اعتماد الطبيب
- **تشفير كامل لملفات المرضى** (نفس منطق AES-256 اللي استخدمناه في مشروع الصيانة لبيانات الأجهزة الحساسة، هنا أهم بكتير لأنها بيانات صحية)

### كود أولي (نقطة بداية)
```python
# services/clinical_notes_service/transcribe.py
import whisper

model = whisper.load_model("medium")  # يدعم العربي بشكل معقول من غير fine-tuning

def transcribe_consultation(audio_path: str) -> str:
    result = model.transcribe(audio_path, language="ar")
    return result["text"]
```

```python
# services/clinical_notes_service/structure_note.py
STRUCTURE_PROMPT = """
أنت مساعد طبي. حوّل النص التالي (منطوق أثناء كشف طبي بالعامية المصرية)
إلى ملاحظة طبية منظمة بالحقول التالية فقط: الشكوى، الفحص، التشخيص المبدئي، الروشتة، الخطة العلاجية.
لا تضف أي تشخيص أو معلومة لم يذكرها الطبيب صراحة. اترك الحقل فارغًا لو غير مذكور.

النص: {transcript}
"""
# استدعاء Claude API بالبرومبت ده، والنتيجة تتعرض للطبيب كمسودة قابلة للتعديل قبل الحفظ
```

---

## 3. الميزة الثانية: تنبؤ الغياب + تذكير واتساب ذكي

### الفكرة
النظام بيحدد المواعيد الأكتر عرضة للغياب، ويبعت تذكير واتساب إضافي في التوقيت الأنسب للمواعيد دي تحديدًا.

### مرحلة البداية (بيانات غير كافية للتدريب)
**تنويه مهم يطبق نفس منطق مشروع العقارات:** في البداية مفيش بيانات كافية لتدريب موديل حقيقي. يبدأ بقواعد بسيطة (Heuristic) موسومة بوضوح كمؤقتة:
```python
# services/noshow_service/heuristic_v1.py
# PLACEHOLDER LOGIC - HEURISTIC ONLY - REPLACE WITH TRAINED MODEL AFTER 200+ REAL APPOINTMENTS

def estimate_noshow_risk(appointment: dict) -> str:
    risk_score = 0
    if appointment["is_first_visit"]:
        risk_score += 2
    if appointment["day_of_week"] in ["Friday", "Saturday"]:
        risk_score += 1
    if appointment["lead_time_days"] > 7:
        risk_score += 1
    if appointment["patient_past_noshow_count"] > 0:
        risk_score += 3

    return "HIGH" if risk_score >= 4 else "MEDIUM" if risk_score >= 2 else "LOW"
```
بعد تجميع 200+ موعد حقيقي (حضور/غياب فعلي)، يتم تدريب موديل حقيقي (Logistic Regression بسيط كافي في البداية) يستبدل الـHeuristic، مع `/retrain` endpoint زي منطق مشروع العقارات بالظبط.

### التذكير الذكي عبر واتساب
**إعادة استخدام مباشرة** لخدمة Baileys اللي بنيناها في مشروع الصيانة (نفس البنية: خدمة مستقلة على Render + جلسة محفوظة في Postgres). المواعيد المصنّفة HIGH بتاخد رسالة تأكيد إضافية قبل الموعد بـ24 ساعة وساعتين، بدل رسالة واحدة عادية لكل المواعيد.

---

## 4. الوظائف الأساسية (Parity مع المنافسين)
- حجز مواعيد + تقويم أطباء
- ملف مريض (تاريخ مرضي، روشتات، أشعة، تحاليل)
- فواتير ومدفوعات
- دعم دفع نقدي ومختلط (زي الميزة اللي ClinicGateway بيركز عليها كخاصية مصرية أساسية)

---

## 5. الهوية البصرية المميزة (نفس فلسفة مشروع الصيانة، سياق مختلف)

### العنصر المادي الحقيقي: الروشتة
زي ما التذكرة الحرارية كانت أساس هوية مشروع الصيانة، هنا العنصر المادي الحقيقي اللي المريض بياخده في إيده هو **الروشتة**. التصميم كله يدور حولها.

### الألوان
| الاستخدام | اللون | السبب |
|---|---|---|
| خلفية | كريمي دافئ فاتح `#FAF7F2` | بديل عن الأبيض البارد المستشفياتي التقليدي |
| أساسي | أخضر مريّح غامق `#2D5A4A` | لون الثقة الطبية من غير كليشيه "الأزرق الطبي" المكرر في كل مكان |
| Accent وحيد | تركواز دافئ `#4A9B8E` | يُستخدم في عنصر واحد بس لكل شاشة (الفعل الأساسي) |
| تحذير | كهرماني `#C77D2E` | حالات الغياب المتوقع (HIGH risk) |

### التوقيع البصري: "بطاقة الروشتة"
كل روشتة أو ملاحظة طبية معتمدة تتصمم كبطاقة بحواف مقصوصة خفيفة (Perforated edge illustration) وخط واضح جدًا للقراءة (مش زخرفي)، تتكرر في كل شاشة فيها بيانات طبية — نفس منطق "تاج الأمر" في مشروع الصيانة، لكن هنا الشكل مستوحى من الروشتة الورقية الحقيقية.

### ممنوعات (نفس قائمة مشروع الصيانة)
- بدون تدرجات لونية (gradients)
- بدون إيموجي في نصوص الواجهة
- بدون تطابق كامل بين الكروت الإحصائية — كسّر النمط بشريط حالة ممتد بدل كروت متطابقة

---

## 6. نطاق العمل والمراحل

| المرحلة | المحتوى |
|---|---|
| 1 | الوظائف الأساسية (حجز، ملف مريض، فواتير) — أساس لازم يشتغل الأول |
| 2 | ملاحظات الصوت بالعربي (مع كل متطلبات الموافقة والخصوصية) |
| 3 | تنبؤ الغياب (Heuristic أولًا) + تذكير واتساب ذكي (إعادة استخدام Baileys) |
| 4 | الهوية البصرية المميزة الكاملة |

---

## 7. اختبار القبول
1. تسجيل صوتي لكشف تجريبي → يطلع ملاحظة منظمة قابلة للتعديل، الطبيب لازم يعتمدها قبل الحفظ النهائي
2. شاشة الموافقة على التسجيل تظهر إجباريًا قبل أي تسجيل، ومفيش طريقة تتخطاها
3. الملف الصوتي الخام يتحذف من التخزين بعد التحويل للنص بنجاح (تحقق من عدم وجوده في الـstorage بعد المعالجة)
4. مواعيد HIGH risk تاخد رسالة واتساب إضافية، LOW/MEDIUM ماياخدوش
5. الهوية البصرية خالية من أي تدرج لوني أو إيموجي، وعنصر "بطاقة الروشتة" متكرر بثبات في كل شاشة فيها بيانات طبية
