# مهمة: بناء نظام التصميم والهوية البصرية — روشتة (Roshetta)

## 1. نظام الألوان — CSS Variables

```css
:root {
  --font-cairo: 'Cairo', sans-serif;
  --font-mono-data: 'JetBrains Mono', 'Courier New', monospace;

  --background: 38 44% 97%;        /* #FAF7F2 - كريمي دافئ، بديل الأبيض المستشفياتي البارد */
  --foreground: 60 2% 17%;         /* #2C2C2A */

  --card: 0 0% 100%;
  --card-foreground: 60 2% 17%;

  --primary: 159 33% 27%;          /* #2D5A4A - أخضر ثقة طبي، بديل "الأزرق الطبي" المكرر */
  --primary-foreground: 0 0% 100%;

  --accent: 170 35% 45%;           /* #4A9B8E - تركواز دافئ، استخدام محدود لعنصر واحد بس لكل شاشة */
  --accent-foreground: 0 0% 100%;

  --warning: 31 62% 48%;           /* #C77D2E - كهرماني، لحالات الغياب المتوقع (HIGH risk) فقط */
  --warning-foreground: 0 0% 100%;

  --destructive: 0 65% 48%;
  --destructive-foreground: 0 0% 100%;

  --border: 43 21% 87%;            /* #E4E0D6 */
  --input: 43 21% 87%;
  --ring: 170 35% 45%;
  --radius: 10px;
}
```

**القاعدة الإلزامية:** لون الـaccent (التركواز) يظهر في عنصر واحد بس لكل شاشة — زرار الفعل الأساسي أو حالة نجاح واحدة. أي استخدام متكرر له في نفس الشاشة يُعتبر خرق لقاعدة التصميم ولازم يُصحح.

---

## 2. التايبوجرافي

```css
@import url('https://fonts.googleapis.com/css2?family=Cairo:wght@400;600;700&family=JetBrains+Mono:wght@400;600;700&display=swap');
```

- **Cairo**: كل النصوص العربية العامة (عناوين، أزرار، نصوص واجهة)
- **JetBrains Mono**: كل البيانات الرقمية والأكواد بثبات تام — رقم الروشتة (RX-XXXX)، أرقام الهواتف، الأسعار، التواريخ، أرقام الملفات الطبية. لا استثناء لأي رقم يظهر في أي شاشة.

في `tailwind.config.js`:
```js
fontFamily: {
  sans: ["var(--font-cairo)", "system-ui", "sans-serif"],
  mono: ["JetBrains Mono", "Courier New", "monospace"],
}
```

---

## 3. اللوجو والهوية

### اللوجوتايب
بطاقة بحواف مقصوصة (نقط دائرية أعلى البطاقة توحي بتذكرة قابلة للفصل)، خط متقطع كحدود، كلمة "روشتة" بخط Cairo Bold 700 بلون `--primary`، وتحتها "Roshetta" بخط أصغر بلون `--accent`.

```
مواصفات اللوجو:
- الشكل: مستطيل بحدود متقطعة (dashed border 1.5px) بلون --primary
- 3 دوائر صغيرة (10px قطر) بحدود متقطعة أعلى المنتصف — توحي بخط التقطيع في الروشتة الحقيقية
- الخط: Cairo 700، 34px للنص العربي، Cairo 600 بحجم أصغر (13px) للنص اللاتيني تحته
- خط فاصل رفيع (1px، شفافية 15%) بين الاسم والوصف التوضيحي
```

### حجم اللوجو ومساحاته
- الحد الأدنى للعرض: 180px (تحته يفقد وضوح الحدود المتقطعة)
- مساحة الأمان حول اللوجو: مسافة لا تقل عن ارتفاع حرف "ر" في الاسم من كل الجهات

---

## 4. العنصر التوقيعي: بطاقة الروشتة (مكوّن قابل لإعادة الاستخدام)

مكوّن جديد `src/components/ui/rx-tag.tsx` يُستخدم بدل أي عرض نصي عادي لرقم ملف طبي أو روشتة:

```tsx
import React from "react";

export const RxTag: React.FC<{ number: string; className?: string }> = ({ number, className = "" }) => (
  <span
    className={`inline-flex items-center gap-1 rounded-sm border border-dashed border-primary/40 bg-primary/5 px-2.5 py-1 font-mono text-xs font-bold tracking-wide text-primary ${className}`}
  >
    {number}
  </span>
);
```

**يُستخدم في:** أي مكان يظهر فيه رقم روشتة أو رقم ملف مريض — شاشة الحجز، ملف المريض، الفواتير، شاشة الطبيب أثناء الكشف. نفس المكوّن، بلا استثناء، في كل الشاشات.

---

## 5. تطبيق النظام على المكوّنات الأساسية

### الشارات (Badges) — حالة المواعيد
- `rounded-sm` (حواف مربعة خفيفة) بدل `rounded-full` — يتماشى مع منطق "بطاقة" لا "حبة دواء"
- ألوان الحالة: `--accent` (تم التأكيد)، `--warning` (احتمال غياب مرتفع)، `--destructive` (ألغيت)، رمادي محايد (منتظر)

### الكروت الإحصائية (Dashboard)
- بدون تدرجات لونية إطلاقًا
- بدون تطابق كامل بين الكروت — شريط حالة أفقي ممتد بدل 4 صناديق منفصلة متطابقة (نفس منطق مشروع الصيانة، مُثبت أنه بيكسر الشكل الـ"AI-generated" العام)
- الأرقام بخط Monospace كبير، الأيقونات خطية بسيطة بلون واحد (رمادي أو `--primary`)، مش أيقونات ملونة في صناديق مستديرة

### الأزرار
- زرار أساسي واحد بس لكل شاشة بلون `--accent`
- باقي الأزرار: outline أو ghost بلون `--primary` أو رمادي محايد

---

## 6. الممنوعات (قائمة إلزامية على كل الشاشات)
- ❌ بدون تدرجات لونية (gradients) في أي مكان
- ❌ بدون إيموجي في أي نص واجهة
- ❌ بدون تكرار لون الـaccent في أكتر من عنصر واحد بنفس الشاشة
- ❌ بدون أيقونات ملونة داخل صناديق مستديرة (النمط الـ"SaaS metric icon" التقليدي)
- ❌ بدون استخدام "الأزرق الطبي" الكليشيهي كلون أساسي

---

## نطاق العمل
- ملفات جديدة: `src/components/ui/rx-tag.tsx`، مكوّن اللوجو (`src/components/brand/logo.tsx`)
- ملفات معدّلة: `globals.css` (متغيرات الألوان)، `tailwind.config.js` (الخطوط)، `components/ui/badge.tsx` (الشكل والألوان)
- **لا تلمس** أي منطق وظيفي (حجز، ملف مريض، فواتير) — هذه المهمة بصرية بحتة

## اختبار القبول
1. لا يوجد أي تدرج لوني في أي شاشة
2. اللوجو ظاهر بنفس المواصفات (حدود متقطعة، 3 دوائر أعلى المنتصف) في الهيدر بكل الشاشات
3. كل رقم روشتة أو ملف مريض معروض بمكوّن `RxTag` الموحد، بلا استثناء
4. لون الـaccent (التركواز) لا يظهر أكتر من مرة واحدة في نفس الشاشة
5. كل بيانات رقمية (تواريخ، أسعار، أرقام هواتف) بخط Monospace بثبات
